<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Emotional Health AI</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Animation for smooth background transition */
    @keyframes pulse {
      0% { opacity: 0.8; transform: scale(1); }
      50% { opacity: 1; transform: scale(1.05); }
      100% { opacity: 0.8; transform: scale(1); }
    }

    /* Animated floating bubbles */
    @keyframes bubble {
      0% { transform: translateY(0); opacity: 0.7; }
      50% { transform: translateY(-10px); opacity: 1; }
      100% { transform: translateY(0); opacity: 0.7; }
    }

    .bubble {
      animation: bubble 3s ease-in-out infinite;
    }
  </style>
</head>
<body class="bg-gradient-to-r from-pink-500 via-purple-600 to-indigo-800 min-h-screen flex items-center justify-center">

  <div class="bg-white  rounded-xl shadow-xl w-full max-w-md p-10 space-y-8 ">
    <div class="flex justify-center">
      <div class="text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-red-500 to-pink-500">
        <span class="bubble">💖</span>
      </div>
    </div>

    <h1 class="text-center text-4xl font-extrabold text-gray-800">EmotiCare</h1>
    <p class="text-center text-lg text-gray-600">Unlock your potential and embrace emotional well-being with AI.</p>

    <!-- Display error message if any -->
    <% if (error) { %>
      <div class="bg-red-500 text-white p-4 rounded mb-4">
        <p><%= error %></p>
      </div>
    <% } %>
    <form action="/login" method="POST" class="space-y-6">

      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      <div class="relative">
        <label for="email" class="block text-lg font-medium text-gray-700">Email Address</label>
        <input type="email" id="email" name="email" required class="mt-3 block w-full px-6 py-4 border-2 border-gray-300 rounded-lg shadow-sm focus:ring-4 focus:ring-purple-600 focus:outline-none text-gray-800 placeholder-gray-500" placeholder="you@example.com">
      </div>

      <div class="relative">
        <label for="password" class="block text-lg font-medium text-gray-700">Password</label>
        <input type="password" id="password" name="password" required class="mt-3 block w-full px-6 py-4 border-2 border-gray-300 rounded-lg shadow-sm focus:ring-4 focus:ring-purple-600 focus:outline-none text-gray-800 placeholder-gray-500" placeholder="Your secure password">
      </div>

      <button type="submit" class="w-full py-4 px-6 bg-gradient-to-r from-green-400 via-teal-500 to-blue-500 text-white font-bold rounded-lg shadow-lg hover:from-green-500 hover:to-blue-600 transition duration-300">Login</button>
    </form>

    <div class="flex justify-between items-center">
       <p class="text-sm text-gray-500">New here? <a href="/register" class="text-teal-600 hover:text-teal-500">Create an account</a></p>
    </div>

    <div class="text-center">
      <p class="text-sm text-gray-500">Need help? Reach out to us! 💬</p>
    </div>
  </div>

  <!-- Floating Bubbles -->
  <div class="absolute top-10 left-1/3 text-4xl bubble text-pink-400">
    🧠
  </div>
  <div class="absolute top-1/4 right-10 text-5xl bubble text-indigo-500">
    💬
  </div>
  <div class="absolute bottom-0 left-1/4 text-6xl bubble text-yellow-400">
    🌟
  </div>

</body>
</html>
